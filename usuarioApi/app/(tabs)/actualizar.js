import ActualizarUsuariosScreen from '../../screens/ActualizarUsuariosScreen';

// Este archivo crea la ruta /actualizar dentro del grupo de pestañas.
// Su única función es mostrar el componente que contiene el formulario de edición.
export default function Actualizar() {
    return <ActualizarUsuariosScreen />;
}
